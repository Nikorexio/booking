<nav class="bg-white shadow-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <%= link_to "Booking", root_path, class: "text-2xl font-bold text-blue-600" %>
      
      <div class="flex space-x-4">
        <% if user_signed_in? %>
          <%= link_to "Личный кабинет", profile_path, class: "nav-link" %>
          <%= link_to "Выйти", destroy_user_session_path, 
  data: { turbo_method: :delete },
  class: "nav-link text-red-600 hover:text-red-700" %>
        <% else %>
          <%= link_to "Войти", new_user_session_path, class: "nav-link" %>
          <%= link_to "Регистрация", new_user_registration_path, 
                     class: "nav-link bg-blue-100 hover:bg-blue-200" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>